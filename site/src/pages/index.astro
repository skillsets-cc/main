---
export const prerender = true;

import BaseLayout from '@layouts/BaseLayout.astro';
import SkillsetGrid from '@components/SkillsetGrid';
import { getSkillsets } from '@/lib/data';

const skillsets = getSkillsets();
---

<BaseLayout title="Skillsets.cc">
  <main class="max-w-7xl mx-auto px-4 py-12 pb-20">
    <section class="mb-3 text-lg md:text-xl text-text-secondary font-serif leading-relaxed space-y-3">
      <p>
        A <span class="font-bold text-text-ink">skillset</span> is an integrated workflow built on tightly coupled Claude Code primitives—skills, agents, hooks, plugins, and MCP.
      </p>
      <p>
        <span class="font-bold text-text-ink">Skillsets.cc</span> is a community-driven registry of these consolidated workflows.
      </p>
      <p>
        Limited, high-friction submissions that actually shipped, actively maintained and improved by the authors.
      </p>
      <p>
        Install* with <code class="bg-white border border-border-ink px-2 py-1 rounded-none font-mono text-sm">npx skillsets install &lt;name&gt;</code>.
      </p>
      <p>
        Already a pro? We bet your skillset kicks ass. <a href="/contribute" class="underline hover:text-accent-primary">Cohort One, assemble!</a>
      </p>
    </section>

    <div class="sticky top-0 bg-white z-50 -mx-4 px-4 pt-3 pb-0">
      <span class="text-sm font-bold uppercase tracking-wider text-text-tertiary font-mono">Registry</span>
      <p class="text-xs text-text-tertiary mt-1">
        *Skillsets come with all the guarantees of probabilistic space — as with any OSS, verify before you trust.
      </p>
      <div class="border-b-2 border-orange-500 mt-2"></div>
    </div>

    <SkillsetGrid client:load skillsets={skillsets} />
  </main>
</BaseLayout>
