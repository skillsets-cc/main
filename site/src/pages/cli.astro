---
export const prerender = true;

import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="CLI - Skillsets.cc">
  <main class="max-w-4xl mx-auto px-4 py-12">
    <header class="mb-8 border-b border-border-ink pb-6">
      <h1 class="text-4xl font-medium font-serif text-text-ink">CLI Reference</h1>
      <p class="mt-2 text-lg text-text-secondary font-serif">
        The terminal-native way to work with skillsets.
      </p>
    </header>

    <div class="space-y-8">
      <!-- Quick Start -->
      <section class="border border-border-ink p-6 bg-stone-50">
        <h2 class="text-sm font-bold font-mono mb-4 text-text-ink uppercase tracking-wider">Quick Start</h2>
        <div class="space-y-3 font-mono text-sm">
          <div class="flex items-start gap-4">
            <code class="text-orange-500 whitespace-nowrap">npx skillsets list</code>
            <span class="text-text-tertiary">Browse all skillsets</span>
          </div>
          <div class="flex items-start gap-4">
            <code class="text-orange-500 whitespace-nowrap">npx skillsets search "sdlc"</code>
            <span class="text-text-tertiary">Find by keyword</span>
          </div>
          <div class="flex items-start gap-4">
            <code class="text-orange-500 whitespace-nowrap">npx skillsets install @user/name</code>
            <span class="text-text-tertiary">Install to current directory</span>
          </div>
        </div>
      </section>

      <!-- Discovery Commands -->
      <section>
        <h2 class="text-2xl font-bold text-text-ink mb-4 font-serif">Discovery</h2>
        <div class="space-y-6">
          <div class="border-b border-border-ink pb-4">
            <h3 class="font-mono font-bold text-text-ink mb-2">list</h3>
            <p class="text-lg text-text-secondary font-serif mb-3">Browse all available skillsets in the registry.</p>
            <div class="bg-stone-50 border border-border-ink p-4 font-mono text-sm space-y-2">
              <div><code>npx skillsets list</code></div>
              <div><code>npx skillsets list --sort stars</code></div>
              <div><code>npx skillsets list --limit 5 --json</code></div>
            </div>
          </div>

          <div class="border-b border-border-ink pb-4">
            <h3 class="font-mono font-bold text-text-ink mb-2">search</h3>
            <p class="text-lg text-text-secondary font-serif mb-3">Fuzzy search by name, description, or tags.</p>
            <div class="bg-stone-50 border border-border-ink p-4 font-mono text-sm space-y-2">
              <div><code>npx skillsets search "adversarial review"</code></div>
              <div><code>npx skillsets search "sdlc" --tags multi-agent</code></div>
              <div><code>npx skillsets search "planning" --limit 3</code></div>
            </div>
          </div>
        </div>
      </section>

      <!-- Installation Commands -->
      <section>
        <h2 class="text-2xl font-bold text-text-ink mb-4 font-serif">Installation</h2>
        <div class="space-y-6">
          <div class="border-b border-border-ink pb-4">
            <h3 class="font-mono font-bold text-text-ink mb-2">install</h3>
            <p class="text-lg text-text-secondary font-serif mb-3">Install a skillset to the current directory. Verifies checksums automatically. No .git folders, no bloat.</p>
            <div class="bg-stone-50 border border-border-ink p-4 font-mono text-sm space-y-2">
              <div><code>npx skillsets install @supercollectible/Valence</code></div>
              <div><code>npx skillsets install @user/name --force</code> <span class="text-text-tertiary"># overwrite existing</span></div>
              <div><code>npx skillsets install @user/name --backup</code> <span class="text-text-tertiary"># backup first</span></div>
            </div>
          </div>

        </div>
      </section>

      <!-- Contribution Commands -->
      <section>
        <h2 class="text-2xl font-bold text-text-ink mb-4 font-serif">Contribution</h2>
        <p class="text-lg text-text-secondary font-serif mb-4">
          Commands for skillset authors. See <a href="/contribute" class="text-orange-500 hover:underline">Contribute</a> for the full workflow.
        </p>
        <div class="space-y-6">
          <div class="border-b border-border-ink pb-4">
            <h3 class="font-mono font-bold text-text-ink mb-2">init</h3>
            <p class="text-lg text-text-secondary font-serif mb-3">Scaffold a new skillset submission with the required structure.</p>
            <div class="bg-stone-50 border border-border-ink p-4 font-mono text-sm">
              <code>npx skillsets init</code>
            </div>
          </div>

          <div class="border-b border-border-ink pb-4">
            <h3 class="font-mono font-bold text-text-ink mb-2">audit</h3>
            <p class="text-lg text-text-secondary font-serif mb-3">Validate your skillset before submission. Generates AUDIT_REPORT.md.</p>
            <div class="bg-stone-50 border border-border-ink p-4 font-mono text-sm">
              <code>npx skillsets audit</code>
            </div>
          </div>

          <div class="border-b border-border-ink pb-4">
            <h3 class="font-mono font-bold text-text-ink mb-2">submit</h3>
            <p class="text-lg text-text-secondary font-serif mb-3">Open a PR to the registry. Requires <code class="bg-white border border-border-ink px-1">gh</code> CLI.</p>
            <div class="bg-stone-50 border border-border-ink p-4 font-mono text-sm">
              <code>npx skillsets submit</code>
            </div>
          </div>
        </div>
      </section>

      <!-- Source -->
      <section class="pt-4 border-t border-border-ink">
        <p class="text-text-secondary text-lg font-serif">
          Source: <a href="https://github.com/skillsets-cc/main/tree/main/cli" class="text-orange-500 hover:underline" target="_blank" rel="noopener noreferrer">github.com/skillsets-cc/main/cli</a>
        </p>
      </section>
    </div>
  </main>
</BaseLayout>
